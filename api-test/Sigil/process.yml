info:
  name: process
  type: http
  seq: 1

http:
  method: POST
  url: "{{api}}/video/process"
  body:
    type: multipart-form
    data:
      - name: files
        type: file
        value:
          - D:\Kelvin McClean\Downloads\IMG_1120.MOV
        contentType: video/quicktime
      - name: timestamps
        type: text
        value: 1599729025,1549729058,1549729025,1549729058,1549729025,1549729058
      - name: stabalize
        type: text
        value: "true"
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: bru.setEnvVar("videoJobId", res.body.id)

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
