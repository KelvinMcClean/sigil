info:
  name: process
  type: http
  seq: 1

http:
  method: POST
  url: "{{api}}/video/process"
  body:
    type: multipart-form
    data:
      - name: files
        type: file
        value:
          - C:\Users\Kelvin McClean\Desktop\Pics\Castle Espie\DSC_8375.MP4
        contentType: video/mp4
      - name: timestamps
        type: text
        value: 1549729025,1549729058
      - name: stabalize
        type: text
        value: "true"
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: bru.setEnvVar("jobId", res.body.jobId)

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
